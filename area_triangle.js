// Formula to calculate the Area of a Triangle - (base * height) / 2

const baseValue = 4;
const heightValue = 6;

// calculate the area
const areaValue = (baseValue * heightValue) / 2;

console.log(
  `The area of the triangle with base - ${baseValue} and height - ${heightValue} is ${areaValue}`
);